kind = "web"
previewPath = "/"
title = "ITI EMS - Employment Management System"
version = "1.0.0"
id = "artifacts/iti-ems"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 25859

[services.development]
run = "pnpm --filter @workspace/iti-ems run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/iti-ems", "run", "build" ]
publicDir = "artifacts/iti-ems/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "25859"
BASE_PATH = "/"
